import { StyleSheet, Dimensiosn } from "react-native";

const styles = StyleSheet.create({
    mainText:{
        fontSize: 30,
        fontWeight: 'bold',
        marginTop: 20
    }
})

export default styles